# Sample MCP Security & Trust Report

> **Demo report:** This report was generated against an intentionally vulnerable mock MCP server created to validate the scanner end to end. The credential-like string, prompt-injection language, weak schemas, URL-risk surfaces, and malformed-input behavior were deliberately introduced for testing. The endpoint below is a documentation placeholder, not a live customer system.

**Server:** `https://demo-vulnerable-mcp.example/mcp`  
**Name:** `mock-mcp-server`  
**Scan type:** Static schema analysis + authorized dynamic checks  
**Tools exposed:** 3

## Score: 66/100 (Grade C)

## Summary

- Tools with possible secret leaks: **1**
- Tools with prompt-injection-style language: **1**
- Tools with SSRF-prone parameters: **2**
- Tools with weak or missing schema documentation: **1**
- Confirmed or unclear internal-URL behavior during live testing: **2**
- Tools reachable without authentication: **0**
- Stack-trace leaks triggered by malformed input: **3**

## Findings

- 🔴 **[CRITICAL]** `secret_leak` (`get_weather`): Possible OpenAI-style API key found embedded in tool metadata.
- 🟠 **[HIGH]** `prompt_injection_risk` (`fetch_url`): Tool description contains agent-directed instruction language: `ignore previous instructions`.
- 🟠 **[HIGH]** `prompt_injection_risk` (`fetch_url`): Tool description contains agent-directed instruction language: `always call this tool first`.
- 🟠 **[HIGH]** `ssrf_confirmed_or_unclear` (`fetch_url`): Tool accepted an internal or metadata URL as input and returned HTTP 200 without a validation error. Manual verification is required to determine whether the server actually fetched the URL.
- 🟠 **[HIGH]** `ssrf_confirmed_or_unclear` (`ping`): Tool accepted an internal or metadata URL as input and returned HTTP 200 without a validation error. Manual verification is required to determine whether the server actually fetched the URL.
- 🟡 **[MEDIUM]** `ssrf_prone_param` (`fetch_url`): Parameter `url` accepts a URL or path-like value. If unvalidated server-side, this can enable SSRF or path traversal.
- 🟡 **[MEDIUM]** `ssrf_prone_param` (`ping`): Parameter `host` accepts a URL or path-like value. If unvalidated server-side, this can enable SSRF or path traversal.
- 🟡 **[MEDIUM]** `error_leakage` (`fetch_url`): Malformed input triggered a response resembling a raw stack trace, which can disclose internal paths or framework details.
- 🟡 **[MEDIUM]** `error_leakage` (`get_weather`): Malformed input triggered a response resembling a raw stack trace, which can disclose internal paths or framework details.
- 🟡 **[MEDIUM]** `error_leakage` (`ping`): Malformed input triggered a response resembling a raw stack trace, which can disclose internal paths or framework details.
- 🔵 **[LOW]** `schema_quality` (`ping`): Tool has little or no description, making it harder for agents to use safely and correctly.
- 🔵 **[LOW]** `schema_quality` (`ping`): Parameter `host` has no declared type, creating an ambiguous input contract.

## Interpretation

This demo produced **12 categorized findings across 3 exposed tools**. The result shows how the scanner combines metadata analysis, schema review, and optional live behavioral checks into a single shareable assessment.

The score is a heuristic prioritization aid. A finding does not automatically confirm a vulnerability, and a high score does not certify an MCP server as secure.

---

_Generated by MCP Server Security & Trust Scanner. This is an automated heuristic scan and is not a substitute for a full manual security audit or penetration test._
