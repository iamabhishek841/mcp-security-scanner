# Sample MCP Security & Trust Report

> Generated by a local Actor run against `examples/mock_mcp_server.py`. All
> vulnerable behavior is simulated, the credential pattern is fake, and the
> server makes no outbound or system calls.

**Server:** `http://127.0.0.1:8765/mcp`

**Name:** safe-vulnerable-demo

**Tools exposed:** 3

**Scan mode:** `static_and_dynamic`

**Controlled probe URL used:** yes

## Score: 60/100 (Grade C)

## Execution transparency

- Dynamic tool allowlist: `fetch_preview`, `credential_status`, `format_record`
- Tools tested dynamically: `fetch_preview`, `credential_status`, `format_record`

## Summary

- Tools with possible secret leaks: **1**
- Tools with prompt-injection-style language: **1**
- Tools with URL/path risk surfaces: **1**
- Tools with weak or missing schema documentation: **1**
- Controlled callback accepted without MCP error: **1**
- Tools callable in the unauthenticated comparison: **3**
- Stack-trace-like responses: **1**

## Findings

- **[CRITICAL]** `secret_leak` (`credential_status`): Possible OpenAI-style API key found embedded in tool metadata.
- **[HIGH]** `prompt_injection_risk` (`fetch_preview`): Tool description contains agent-directed instruction language: `ignore previous instructions`.
- **[HIGH]** `prompt_injection_risk` (`fetch_preview`): Tool description contains agent-directed instruction language: `always call this tool first`.
- **[HIGH]** `ssrf_confirmed_or_unclear` (`fetch_preview`): Tool accepted the explicitly supplied controlled callback URL without an MCP error. Confirm callback receipt before treating this as SSRF; acceptance alone does not prove a fetch.
- **[HIGH]** `missing_auth` (`fetch_preview`): Tool call succeeded in a separately initialized session without the Authorization header supplied for the authenticated scan. Verify whether public access is intentional.
- **[HIGH]** `missing_auth` (`credential_status`): Tool call succeeded in a separately initialized session without the Authorization header supplied for the authenticated scan. Verify whether public access is intentional.
- **[HIGH]** `missing_auth` (`format_record`): Tool call succeeded in a separately initialized session without the Authorization header supplied for the authenticated scan. Verify whether public access is intentional.
- **[MEDIUM]** `ssrf_prone_param` (`fetch_preview`): Parameter `url` accepts a URL/path-like value. If unvalidated server-side, this can enable SSRF or path traversal.
- **[MEDIUM]** `error_leakage` (`format_record`): Malformed input triggered a response resembling a raw stack trace, which can leak internal paths or framework details.
- **[LOW]** `schema_quality` (`format_record`): Parameter `payload` has no declared type or equivalent schema contract.

## Limitations

- Findings are heuristic and may include false positives or false negatives.
- The scanner does not inspect server source code or downstream systems.
- A successful tool response does not prove that a supplied callback URL was fetched.

---

_Generated by MCP Security Scanner. This automated heuristic scan is not a substitute for manual security review._
